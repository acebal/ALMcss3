ALMCSS.logging = function() {

	var

	var Logger = function(name, level) {

		var isEnabled = true;

		var format = function(level, message) {
			return name + '[' + level + ']: ' + message;
		};

		var

	};

}();